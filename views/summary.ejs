<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Summary</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
    <style>
        .bg {
            position: absolute;
            z-index: -1;
            top: 0;
            right: 0;
            bottom: 0;
            left: 0;
            background: linear-gradient(135deg, #17e9ad, #1d18d0);
        }

        .content-wrapper {
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            opacity: 0;
            transition: opacity 2s ease;
        }

        .form-signin {
            background-color: rgba(0, 0, 0, 0.7);
            color: #fff;
            padding: 30px;
            border-radius: 10px;
            max-width: 600px;
            width: 100%;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.4);
        }

        .loading-screen {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.85);
            color: #fff;
            display: flex;
            justify-content: center;
            align-items: center;
            font-size: 1.5rem;
            z-index: 9999;
            transition: opacity 1s ease;
        }

        .hidden {
            display: none;
        }

        .fade-in {
            opacity: 0;
            animation: fadeIn 2s forwards;
        }

        @keyframes fadeIn {
            to { opacity: 1; }
        }

        .btn-custom {
            background-color: #17e9ad;
            color: #fff;
            border: none;
            padding: 10px 20px;
            border-radius: 50px;
            box-shadow: 0 4px 15px rgba(23, 233, 173, 0.5);
            transition: background-color 0.3s ease, box-shadow 0.3s ease;
        }

        .btn-custom:hover {
            background-color: #14c08f;
            box-shadow: 0 4px 20px rgba(23, 233, 173, 0.7);
        }
    </style>
</head>
<body>
    <div class="bg"></div>

    <div class="loading-screen" id="loading-screen">
        <div class="spinner"></div>
        <p>Summary Generated!</p>
    </div>

    <div class="content-wrapper" id="content-wrapper">
        <main class="form-signin text-center" id="content">
            <h1 class="h3 fade-in">Summary</h1>
            <p class="fade-in">
                <%= summary %>
            </p>
            <a href="/" class="btn btn-custom mt-4 fade-in">Go back to Home</a>
            <p class="copyright mt-4 fade-in">© SDTech 2024</p>
        </main>
    </div>

    <script>
        window.addEventListener('load', function () {
            setTimeout(function () {
                const loadingScreen = document.getElementById('loading-screen');
                loadingScreen.style.opacity = '0';

                setTimeout(function () {
                    loadingScreen.classList.add('hidden');
                }, 1000);

                const contentWrapper = document.getElementById('content-wrapper');
                contentWrapper.style.opacity = '1';
            }, 2000);
        });
    </script>
</body>
</html>
